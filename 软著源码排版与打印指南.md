# 《浮岛浮光》计算机软件著作权 —— 源代码排版与提交标准指南

为了保证您在申请“计算机软件著作权（软著）”时**一次性审核通过，绝不因排版或行数问题被驳回**，我已按照**中国版权保护中心（国家版权局）**的最新标准，为您导出了两个专属且已深度清洗（去除了多余空行、统一缩进、附带模块清晰说明）的源码文件：

---

## 📂 导出的文件说明（位于项目根目录下 `/Users/qianlongwang/Documents/cocos/flow_land_light/`）

1. **`软著申请_标准60页源码提交(前30后30页共3000行).txt`** （推荐直接用这个提交！）
   * **适用标准**：按照版权中心《计算机软件登记办法》：“源程序行数超过 3000 行的，只需提交前 30 页和后 30 页（共 60 页，每页不少于 50 行，正好 3000 行）”。
   * **内容结构**：精确截取了核心框架开头前 1500 行，以及核心交互与微信 API 结尾后 1500 行。已去除所有空行，保证每页排满绝对不低于 50 行！
2. **`软著申请_全量干净源码备份(共4127有效行).txt`** （备用备份）
   * **适用标准**：包含了项目全量 17 个 TypeScript 源码文件的全部 4127 行有效纯净代码。如果代理机构要求提供全量代码或您想自行调整页数，可随时使用。

---

## 🛠️ 在 Word / WPS 中的“一次通关”排版操作步骤

请将 `软著申请_标准60页源码提交(前30后30页共3000行).txt` 文件内容全选复制，粘贴到新建的 **Microsoft Word** 或 **WPS Office** 文档中，并严格按以下 5 步简单设置：

### 第 1 步：页面布局设置
* **纸张大小**：A4
* **页边距**：上 `2.0 cm`、下 `2.0 cm`、左 `2.0 cm`、右 `2.0 cm`（通常为 Word 的“窄页边距”或普通微调即可）。

### 第 2 步：字体与字号设置（核心控制每页行数）
* 全选代码（`Ctrl + A` / `Cmd + A`）：
* **西文/代码字体**：选择 **`Consolas`** 或 **`Courier New`**（等宽代码字体，防止英文错位）。
* **中文说明字体**：选择 **`宋体`** 或 **`微软雅黑`**。
* **字号**：设为 **`小五`**（或 `9 pt` ~ `9.5 pt`）。**特别注意：用小五号字能保证每页轻松排满 50 ~ 53 行！**

### 第 3 步：段落与行距设置
* 段落行间距（行距）：选择 **`单倍行距 (1.0)`** 或 **`固定值 12 pt`**。
* 段前、段后间距：全部设置为 **`0 pt`**。
* *检查微调*：调完后翻看一下底部页码，正常情况下会完美处于 **58 ~ 60 页**之间。只要总页数不超过 60 页、每页不低于 50 行，即达到 100% 完美过审标准！

### 第 4 步：页眉设置（必须按官方规范标注）
* 双击文档顶部进入页眉设置：
* **左对齐（或居中）填入**：`浮岛浮光休闲益智小游戏 V1.0.0`
* 字体设为 `五号 宋体`。

### 第 5 步：页脚/页码设置（连续页码）
* 双击文档底部进入页脚设置：
* **右上角（或底部居中）**插入页码：直接显示连续阿拉伯数字 `1`、`2`、`3` ... 至 `60`。
* **绝对不要**使用 `第一页` 或 `Page 1 of 60` 这种复杂格式，纯数字页码最容易通过。

---

## ✅ 排版自查对照表（打印前看一眼）

| 检查项目 | 标准规范要求 | 本次导出的对应保障 |
| :--- | :--- | :--- |
| **空行与注释** | 页面内空行过多会被驳回 | 本次导出的 `.txt` 已自动删除所有空白行，完全密实排版 |
| **每页行数** | **必须不少于 50 行** | 使用 9pt/小五号字 + 单倍行距，每页稳定在 51~53 行 |
| **页眉名称** | 全文必须统一且带版本号 | `浮岛浮光休闲益智小游戏 V1.0.0` |
| **页码范围** | 首页为 `1`，末页最多 `60` | 前 1500 行 + 后 1500 行恰好填满 60 页 |

搞定上述设置后，直接导出为 `PDF` 或直接打印，您的源程序材料就大功告成了！准备进入下一步：截图编写操作说明书！
